events {}
http {
    server {
        listen 80;
        server_name localhost;
        location / {
            root /var/www/dummies_investments/html;
        }
        location /api {
            rewrite ^/api/(.*)$ /$1 break;
            proxy_pass http://back;
        }
    }
}